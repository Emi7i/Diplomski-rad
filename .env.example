# Pi SSH login (LAN only)
PI_HOST=192.168.1.50
PI_PORT=22
PI_USERNAME=pi
PI_PASSWORD=changeme

# Backend
BACKEND_PORT=3000
RTMP_PORT=1935
MEDIA_HTTP_PORT=8000
STREAM_APP=live
STREAM_KEY=pi

# Frontend url, uses it as the only allowed CORS origin.
FRONTEND_URL=http://localhost:5173

# Frontend (only VITE_-prefixed vars reach the browser bundle)
VITE_API_URL=http://localhost:3000/api
VITE_WS_URL=ws://localhost:3000/ws
VITE_MEDIA_URL=http://localhost:8000
VITE_STREAM_PATH=live/pi
